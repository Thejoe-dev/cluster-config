apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: podinfo
  namespace: default
spec:
  rules:
    - host: 
      http:
        paths:
          - path: /info
            pathType: Prefix
            backend:
              service:
                name:  podinfo
                port: 
                  number: 9898