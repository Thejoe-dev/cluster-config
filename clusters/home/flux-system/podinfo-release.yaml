apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: default
spec:
  chartRef:
    kind: HelmChart
    name: podinfo
    namespace: default
  interval: 10m
  values:
    replicaCount: 2