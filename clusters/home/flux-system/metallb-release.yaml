apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metallb
spec:
  chartRef:
    kind: GitRepository
    name: metallb
    namespace: flux-system
  interval: 10m
  values:
    replicaCount: 2